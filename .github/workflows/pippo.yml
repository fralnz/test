name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  action-environment-debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
    # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      - run: npm install --global surge
      - run: surge --version
      - run: date >> README.md
      - run: npm install
        working-directory: ./web-frontend/test/
      - run: npm run build
        working-directory: ./web-frontend/test/
      - name: surge deploy
        working-directory: ./web-frontend/test/
        run: | 
          export SURGE_TOKEN=17659243dbcf744580a431068c1641e3
          surge . domineering-balance.surge.sh
      - name: Test run
        run: | 
          date
          ls -la
          pwd 
          echo "CIAO---------------------------------"
          env
      - run: echo "Fineeeeeeeeeeeeeeeeeeeeeeeeeeee"